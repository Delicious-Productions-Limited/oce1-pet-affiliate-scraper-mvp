export const metadata = { title: 'Pet Affiliate Scraper', description: 'Daily pet deals scraped & curated — never miss a deal for your furry friend' }
export default function RootLayout({ children }) {
  return <html lang="en"><body style={{fontFamily:'system-ui',margin:0}}>{children}</body></html>
}
